<section class="diff" aria-label="How we're different">
  <div class="wrap">
    <h2>How is this different from a typical case study agency?</h2>
    <div class="body">
      <p>Most case study agencies are still producing the same artefact they were producing in 2015. Customer story, three quotes, a metric or two, published, and forgotten.</p>
      <p>We produce something measurably different:</p>
      <ul>
        <li><strong>Built to rank on Google</strong> — question-shaped headings, internal links, semantic HTML.</li>
        <li><strong>Built to be cited by ChatGPT</strong> — Atomic Answer block, named attribution, dated metrics, compound schema.</li>
        <li><strong>Built to be read in 45 seconds</strong> — headline result up front, named voices throughout, narrative pulled through the page.</li>
      </ul>
      <p>Same source material. A different deliverable.</p>
    </div>
  </div>
</section>

<style>
  .diff {
    padding: 120px 0;
    background: var(--bg);
    border-bottom: 1px solid var(--rule);
  }
  h2 {
    font-family: var(--serif);
    font-weight: 500;
    font-size: clamp(36px, 4.5vw, 56px);
    line-height: 1.05;
    letter-spacing: -0.02em;
    margin: 0 0 48px;
    max-width: 22ch;
  }
  .body {
    max-width: 64ch;
  }
  .body p {
    font-size: 19px;
    line-height: 1.55;
    color: var(--ink);
    margin: 0 0 24px;
  }
  .body p:last-child {
    margin-bottom: 0;
  }
  ul {
    list-style: none;
    padding: 0;
    margin: 0 0 24px;
  }
  li {
    font-family: var(--serif);
    font-size: 19px;
    line-height: 1.55;
    color: var(--ink);
    padding: 12px 0;
    border-bottom: 1px solid var(--rule);
  }
  li:first-child {
    border-top: 1px solid var(--rule);
  }
  li :global(strong) {
    font-weight: 600;
  }
</style>
