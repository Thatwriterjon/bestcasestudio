---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/components/Navbar.astro
  - src/components/Hero.astro
autonomous: false
requirements:
  - NAV-01
  - HERO-01
  - HERO-02
  - HERO-03

must_haves:
  truths:
    - "The nav bar contains a pink-styled 'Credibility Scorecard' button that opens scorecard.bestcasestud.io in a new tab"
    - "The hero headline reads exactly: 'Your current customers are your most effective salespeople'"
    - "The hero subheading explains case studies for B2B SaaS across website, social, and sales materials"
    - "A 'Book Discovery Call' CTA links to the Calendly URL in the same tab"
    - "A 'Credibility Scorecard' CTA in the hero links to scorecard.bestcasestud.io in a new tab"
    - "No dead anchor links appear in the nav (no links to sections that don't exist yet in Phase 1)"
  artifacts:
    - path: "src/components/Navbar.astro"
      provides: "Nav with Scorecard button and clean Phase-1 link set"
      contains: "scorecard.bestcasestud.io"
    - path: "src/components/Hero.astro"
      provides: "Hero section with locked headline, subheading, and two CTAs"
      contains: "Your current customers are your most effective salespeople"
  key_links:
    - from: "src/components/Hero.astro"
      to: "https://calendly.com/thatwriterjon/best-case-studio"
      via: "Book Discovery Call CTA href"
      pattern: "calendly.com/thatwriterjon"
    - from: "src/components/Hero.astro"
      to: "https://scorecard.bestcasestud.io"
      via: "Credibility Scorecard CTA href"
      pattern: "scorecard.bestcasestud.io"
    - from: "src/components/Navbar.astro"
      to: "https://scorecard.bestcasestud.io"
      via: "Scorecard nav button href"
      pattern: "scorecard.bestcasestud.io"
---

<objective>
Update the Positivus nav and hero components with BCS brand content: the Credibility Scorecard nav button and the locked hero copy with two working CTAs.

Purpose: Delivers the above-the-fold pitch. After this plan, the site communicates the core BCS value proposition to anyone who visits.
Output: Nav and Hero components updated with exact copy, correct links, and correct colour styling using the pink/yellow palette established in Plan 02.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@old_website.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Navbar — add Scorecard button, strip dead anchors</name>
  <files>src/components/Navbar.astro</files>
  <action>
    Open `src/components/Navbar.astro` (or equivalent — find the nav component in the template).

    Changes to make:

    1. Replace the template's existing nav CTA button with a "Credibility Scorecard" button:
       - Text: "Credibility Scorecard"
       - href: `https://scorecard.bestcasestud.io`
       - target: `_blank`
       - rel: `noopener noreferrer`
       - Styling: pink background (#ff7ae7) using the Tailwind class established in Plan 02, dark text for contrast

    2. Nav link handling (Claude's discretion per CONTEXT.md):
       Phase 1 only has the hero section. Remove or comment out any nav links that anchor to sections not yet built (Sponsors, Services, Pricing, Process, Team, Testimonials, Contact). This avoids broken anchor UX.
       Keep the BCS logo/wordmark link (should go to `#` or `/` for now).
       If the template has a mobile menu, apply the same changes there.

    3. Do NOT change the nav layout, hamburger behaviour, or any structural HTML — only swap links and the CTA button content.

    Exact copy to use:
    - Nav CTA button: "Credibility Scorecard"
    - Do not add any other new nav items
  </action>
  <verify>
    <automated>cd /Users/jon/bestcasestud.io && grep -n "scorecard.bestcasestud.io" src/components/Navbar.astro && grep -n "Credibility Scorecard" src/components/Navbar.astro</automated>
  </verify>
  <done>Navbar.astro contains "Credibility Scorecard" linked to scorecard.bestcasestud.io with target="_blank"; no broken anchors to non-existent sections</done>
</task>

<task type="auto">
  <name>Task 2: Update Hero — locked headline, subheading, and two CTAs</name>
  <files>src/components/Hero.astro</files>
  <action>
    Open `src/components/Hero.astro` (or equivalent — find the hero section component in the template).

    Replace all template placeholder copy with BCS copy. All copy is LOCKED (from CONTEXT.md and old_website.md):

    **Headline (exact):**
    "Your current customers are your most effective salespeople"

    **Subheading (exact, from old_website.md and CONTEXT.md):**
    "Case studies build trust for B2B SaaS companies by showcasing how you've already helped your existing customers. On your website, on social media, and in your sales materials. Instead of a boring PDF no-one reads, showcase your case studies on landing pages and with video content."

    **CTA 1 — Primary:**
    - Text: "Book Discovery Call"
    - href: `https://calendly.com/thatwriterjon/best-case-studio`
    - Opens in SAME tab (no target="_blank")
    - Styling: pink background (#ff7ae7) using the Tailwind class from Plan 02

    **CTA 2 — Secondary:**
    - Text: "Credibility Scorecard"
    - href: `https://scorecard.bestcasestud.io`
    - target: `_blank`
    - rel: `noopener noreferrer`
    - Styling: outlined or secondary style — yellow (#ffe56e) border or background, or a ghost/outline variant

    Remove any template placeholder imagery description text or subheadings that don't match the above. Preserve the hero's structural layout (image area, text area, etc.) — only change the copy and CTA links.

    The hero image/illustration area can remain as-is (template's default graphic is acceptable for Phase 1).
  </action>
  <verify>
    <automated>cd /Users/jon/bestcasestud.io && grep -n "Your current customers are your most effective salespeople" src/components/Hero.astro && grep -n "calendly.com/thatwriterjon" src/components/Hero.astro && grep -n "scorecard.bestcasestud.io" src/components/Hero.astro</automated>
  </verify>
  <done>Hero.astro contains exact locked headline, full subheading, Book Discovery Call CTA (Calendly), and Credibility Scorecard CTA (scorecard.bestcasestud.io, new tab)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm nav and hero content</name>
  <action>Human confirms nav Scorecard button and hero copy are correct, all links work</action>
  <what-built>Nav and Hero sections updated with BCS brand content — Credibility Scorecard nav button, locked headline, subheading, and two working CTAs</what-built>
  <how-to-verify>
    1. Run `npm run dev` in /Users/jon/bestcasestud.io
    2. Open http://localhost:4321 in your browser
    3. Check the nav bar:
       - "Credibility Scorecard" button is visible with pink styling
       - Clicking it opens scorecard.bestcasestud.io in a new tab
       - No nav links to sections that don't exist yet (no broken anchors)
    4. Check the hero section:
       - Headline reads: "Your current customers are your most effective salespeople"
       - Subheading mentions case studies, B2B SaaS, website/social/sales materials
       - "Book Discovery Call" button is present and pink — clicking opens Calendly in the same tab
       - "Credibility Scorecard" button is present — clicking opens scorecard.bestcasestud.io in a new tab
    5. Confirm no green is visible anywhere on the page
  </how-to-verify>
  <resume-signal>Type "approved" if all the above look correct, or describe any issues with copy, links, or colours</resume-signal>
</task>

</tasks>

<verification>
All three grep checks pass (headline, Calendly URL, Scorecard URL in Hero.astro). Scorecard URL confirmed in Navbar.astro. Human visual check approved.
</verification>

<success_criteria>
- Navbar.astro: "Credibility Scorecard" linked to scorecard.bestcasestud.io in new tab, pink styling, no dead anchors
- Hero.astro: exact locked headline present, full subheading present, both CTAs present with correct hrefs and target attributes
- `npm run dev` starts without errors
- Human confirms the above-the-fold pitch communicates the BCS core value
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
