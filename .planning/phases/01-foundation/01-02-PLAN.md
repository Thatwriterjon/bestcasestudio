---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tailwind.config.mjs
  - src/components/
autonomous: false
requirements:
  - SETUP-02

must_haves:
  truths:
    - "No green (#B9FF66 or any similar green) is visible anywhere on the rendered site"
    - "Pink (#ff7ae7) appears on buttons, CTAs, and interactive elements"
    - "Yellow (#ffe56e) appears on section backgrounds, tags, badges, and decorative blocks"
    - "The site still renders correctly after all colour replacements"
  artifacts:
    - path: "tailwind.config.mjs"
      provides: "Updated Tailwind palette with pink and yellow replacing green"
      contains: "ff7ae7"
    - path: "src/components/"
      provides: "All component files with hardcoded green hex values replaced"
  key_links:
    - from: "tailwind.config.mjs"
      to: "src/components/"
      via: "Tailwind class names consumed by components"
      pattern: "bg-green|text-green|border-green"
---

<objective>
Replace every instance of the Positivus template's green accent colour with BCS brand colours: pink (#ff7ae7) for primary interactive elements and yellow (#ffe56e) for supporting/decorative elements.

Purpose: Establishes the BCS visual identity. All subsequent plans build on top of a correctly-branded template — no green should survive into Phase 2 or 3.
Output: Tailwind config updated, all component files scrubbed of green hex values and green Tailwind class overrides.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Tailwind config — replace green accent with pink and yellow</name>
  <files>tailwind.config.mjs</files>
  <action>
    Open `tailwind.config.mjs` and replace the Positivus green accent colour entry.

    Colour mapping (locked decisions from CONTEXT.md):
    - Pink #ff7ae7 = primary accent (buttons, CTAs, interactive elements, nav button)
    - Yellow #ffe56e = supporting accent (section backgrounds, tags, badges, decorative block highlights)

    In the Tailwind `theme.extend.colors` or equivalent, wherever the template defines its green accent (typically named something like `accent`, `lime`, `primary`, or a custom name), replace with:
    ```
    pink: '#ff7ae7'   (or use the existing key name for the primary accent)
    yellow: '#ffe56e' (or use the existing key name for the supporting accent)
    ```

    If the template uses a single accent colour entry, split it into two entries (primary/secondary or pink/yellow). Preserve all other colour definitions untouched.

    Verify the config is valid by checking it parses without syntax errors.
  </action>
  <verify>
    <automated>cd /Users/jon/bestcasestud.io && node -e "import('./tailwind.config.mjs').then(c => console.log('Config valid:', JSON.stringify(c.default?.theme?.extend?.colors || {}, null, 2))).catch(e => { console.error('INVALID:', e.message); process.exit(1); })"</automated>
  </verify>
  <done>tailwind.config.mjs contains #ff7ae7 and #ffe56e, no #B9FF66 or green entries remain</done>
</task>

<task type="auto">
  <name>Task 2: Find and replace all hardcoded green hex values and green Tailwind classes in components</name>
  <files>src/components/</files>
  <action>
    The Positivus template may hardcode green hex values (#B9FF66 or similar) or use inline styles inside component files. Scan and replace all of these.

    Step 1 — Find all occurrences:
    ```
    grep -rn "#B9FF66\|#b9ff66\|bg-\[#B9FF66\]\|bg-\[#b9ff66\]" src/ --include="*.astro" --include="*.tsx" --include="*.ts" --include="*.css"
    ```
    Also check for any other green-adjacent hex values (e.g. #A8FF53, #BAFF66) that the template might use as variants.

    Step 2 — Apply colour mapping:
    - Green used as background/fill/decorative → replace with #ffe56e (yellow)
    - Green used as button background, CTA, interactive element, border → replace with #ff7ae7 (pink)
    - When context is ambiguous (e.g. a tag background), use yellow (#ffe56e) as the default

    Step 3 — Also replace any Tailwind green utility classes in component files:
    - `bg-green-*`, `text-green-*`, `border-green-*` → replace with appropriate pink/yellow Tailwind class or inline hex

    Step 4 — Verify no green remains:
    ```
    grep -rn "#B9FF66\|#b9ff66\|bg-green\|text-green\|border-green" src/ --include="*.astro" --include="*.tsx" --include="*.ts" --include="*.css"
    ```
    Output should be empty. If any remain, fix them before completing.

    Do NOT change layout, spacing, or any non-colour CSS properties.
  </action>
  <verify>
    <automated>cd /Users/jon/bestcasestud.io && grep -rn "#B9FF66\|#b9ff66\|bg-green\|text-green\|border-green" src/ --include="*.astro" --include="*.tsx" --include="*.ts" --include="*.css" && echo "GREEN FOUND - FAIL" || echo "No green found - PASS"</automated>
  </verify>
  <done>Zero green hex values or green Tailwind classes remain in src/; site still renders with pink and yellow replacing all green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm colour replacement</name>
  <action>Human visually confirms no green remains and pink/yellow brand colours are applied correctly</action>
  <what-built>Global colour replacement — pink (#ff7ae7) and yellow (#ffe56e) now replace all Positivus green throughout the template</what-built>
  <how-to-verify>
    1. Run `npm run dev` in /Users/jon/bestcasestud.io
    2. Open the site in your browser (typically http://localhost:4321)
    3. Scroll through the entire page
    4. Confirm: no green (#B9FF66 or similar lime/green) is visible anywhere
    5. Confirm: pink appears on buttons and CTAs
    6. Confirm: yellow appears on section backgrounds, tags, or decorative elements
    7. Confirm: the page layout looks correct (colours changed but structure intact)
  </how-to-verify>
  <resume-signal>Type "approved" if colours look correct, or describe any green that remains or layout issues</resume-signal>
</task>

</tasks>

<verification>
Run the grep check: `grep -rn "#B9FF66\|#b9ff66\|bg-green" src/` returns empty. Dev server starts without errors. Visual check confirms no green visible.
</verification>

<success_criteria>
- tailwind.config.mjs contains #ff7ae7 and #ffe56e, zero green entries
- grep for #B9FF66 across src/ returns no matches
- Site renders correctly with pink/yellow brand colours
- Human visual check: approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
