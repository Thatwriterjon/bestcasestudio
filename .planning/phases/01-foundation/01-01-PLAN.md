---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tailwind.config.mjs
  - src/
autonomous: true
requirements:
  - SETUP-01

must_haves:
  truths:
    - "Running `npm run dev` starts a local dev server with no errors"
    - "The Positivus template is visible in the browser at localhost"
    - "All Astro template sections (Hero, Nav, Sponsors, Services, etc.) render without broken imports"
  artifacts:
    - path: "package.json"
      provides: "Astro + Tailwind dependencies"
      contains: "astro"
    - path: "astro.config.mjs"
      provides: "Astro project config"
    - path: "tailwind.config.mjs"
      provides: "Tailwind CSS config with Positivus setup"
    - path: "src/pages/index.astro"
      provides: "Main page assembling all sections"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/"
      via: "Astro component imports"
      pattern: "import.*components"
---

<objective>
Clone the Positivus Astro template and get it running locally with `npm run dev`.

Purpose: Establishes the greenfield project foundation. Every other plan in this phase depends on the template being installed and running.
Output: A working Astro dev server serving the unmodified Positivus template at localhost.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone Positivus template and install dependencies</name>
  <files>package.json, astro.config.mjs, tailwind.config.mjs, src/, public/</files>
  <action>
    Clone the Positivus template from https://github.com/manulthanura/Positivus into the project root at /Users/jon/bestcasestud.io.

    The project root already exists as a git repo — do NOT re-init git. Clone strategy: download the template files directly (use `git clone --no-local` to a temp dir then copy files, or use `degit manulthanura/Positivus .` if degit is available, or `npx degit manulthanura/Positivus .`).

    After files are in place:
    1. Run `npm install` to install all dependencies
    2. Verify the template includes: `src/pages/index.astro`, `src/components/`, `tailwind.config.mjs`, `astro.config.mjs`

    Do not modify any template files in this task — scaffold only.
  </action>
  <verify>
    <automated>cd /Users/jon/bestcasestud.io && ls src/pages/index.astro src/components/ tailwind.config.mjs astro.config.mjs package.json</automated>
  </verify>
  <done>All Positivus template files present in project root, node_modules installed</done>
</task>

<task type="auto">
  <name>Task 2: Verify dev server starts cleanly</name>
  <files>package.json</files>
  <action>
    Run `npm run dev` and confirm it starts without errors. Check that:
    1. The Astro dev server starts (typically on port 4321)
    2. No missing module errors in the console
    3. No broken import errors

    If the dev server starts but has TypeScript or lint warnings (not errors), that is acceptable. Compilation errors that prevent rendering are not acceptable — fix any missing dependency or import errors.

    After confirming the server starts, kill it (Ctrl+C equivalent) before completing this task.
  </action>
  <verify>
    <automated>cd /Users/jon/bestcasestud.io && timeout 20 npm run dev 2>&1 | grep -E "(Local|localhost|error|Error)" | head -20</automated>
  </verify>
  <done>`npm run dev` output contains a localhost URL with no fatal errors — template is running</done>
</task>

</tasks>

<verification>
After both tasks: `npm run dev` starts successfully and the terminal shows a localhost URL. The Positivus template renders in the browser with all sections present.
</verification>

<success_criteria>
- `npm run dev` exits 0 on startup (no fatal errors)
- `src/pages/index.astro` exists and assembles the template sections
- All node_modules installed (package.json present with Astro dependency)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
