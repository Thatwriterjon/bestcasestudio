---
phase: 02-core-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Sponsors.astro
  - src/components/sections/Services.astro
autonomous: false
requirements: [SPONS-01, SPONS-02, SERV-01, SERV-02, SERV-03]

must_haves:
  truths:
    - "A logo strip is visible below the hero with framing copy like 'Brands our founder has worked with' — not 'Our Clients'"
    - "Logos are Jon's personal copywriting client logos, not placeholder template logos"
    - "Three value prop cards are visible: Build Trust, Improve SEO & GEO, Lower CAC"
    - "Each card contains the correct copy from old_website.md"
  artifacts:
    - path: "src/components/sections/Sponsors.astro"
      provides: "Logo strip with framing copy"
      contains: "framing copy attributing logos to Jon's background"
    - path: "src/components/sections/Services.astro"
      provides: "Three value prop cards"
      contains: "Build Trust, Improve SEO & GEO, Lower CAC"
  key_links:
    - from: "src/components/sections/Sponsors.astro"
      to: "src/assets/company/"
      via: "Astro Image imports"
      pattern: "import.*assets/company"
    - from: "src/components/sections/Services.astro"
      to: "src/components/ui/ServiceCard.astro"
      via: "ServiceCard component with index prop"
      pattern: "ServiceCard.*index"
---

<objective>
Replace template placeholder content in the Sponsors and Services sections with real BCS content.

Purpose: Visitors immediately see social proof (Jon's client background) and understand the three core value props case studies deliver.
Output: Logo strip with correct framing copy, three value prop cards with BCS copy.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@old_website.md

<interfaces>
<!-- Key patterns from Phase 1 work. Use these directly. -->

From src/components/ui/ServiceCard.astro:
- index prop (1, 2, or 3) controls card background colour:
  - index=1 → bg-gray (light card, pink heading via `greenhead` class)
  - index=2 → bg-yellow (yellow card, pink heading)
  - index=3 → bg-dark text-gray (dark card, white heading)
- titleTop and titleBottom split the card heading across two lines
- img prop is an imported image asset
- link prop is a URL string (used for "Learn More" arrow link)

From src/components/sections/Sponsors.astro:
- sponsors array: [{ logo: importedSvg, alt: "Company name logo" }]
- Uses Astro Image with class="h-12 w-auto mx-auto"
- Logo container has grayscale filter with hover:grayscale-0
- Section wrapped in <Section id="sponsors">

From src/components/ui/SectionTitle.astro:
- Props: sectionTitle (h2 text), description (paragraph text)
- sectionTitle renders with `greenhead` class (mapped to pink via Phase 1 colour replacement)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provide logo files for Sponsors section</name>
  <files>src/assets/company/</files>
  <action>Human-only step: collect logo asset files from Jon before Claude can wire up the Sponsors section.</action>
  <what-built>Nothing yet — this checkpoint collects the logo assets before Claude wires them up.</what-built>
  <how-to-verify>
    1. Gather SVG or PNG logo files for the companies Jon has done copywriting work for (these go in src/assets/company/)
    2. Name each file clearly: e.g., company-acme.svg, company-notion.svg
    3. Drop them into src/assets/company/ (replacing or alongside the existing placeholder logos company-logo1.svg through company-logo6.svg)
    4. Reply with the list of filenames and the company name each logo represents (used for alt text)

    Note: The existing placeholder logos (Amazon, Dribble, HubSpot, Notion, Netflix, Zoom) are template assets and should NOT be used. If you prefer to keep placeholder logos temporarily, reply "use placeholders" and Claude will use the existing files with updated framing copy only.
  </how-to-verify>
  <verify>Logo files present in src/assets/company/ OR user has replied "use placeholders"</verify>
  <done>Jon has provided logo filenames and company names, or confirmed use of placeholder logos.</done>
  <resume-signal>Reply with filename → company name pairs, or "use placeholders"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update Sponsors section with framing copy and logo list</name>
  <files>src/components/sections/Sponsors.astro</files>
  <action>
    Rewrite Sponsors.astro to:

    1. Add framing copy above the logo grid. The framing copy MUST NOT say "Our Clients" or imply BCS has worked with these companies. Use: "Brands our founder has worked with" as the label text. Render it as a small uppercase tracking-widest text element above the logo grid, centered, in a muted colour (text-zinc-500 or similar).

    2. Update the sponsors array to use the logo files provided in Task 1. Each entry: { logo: importedAsset, alt: "Company name logo" }. If user chose "use placeholders", keep the existing 6 imports but update the alt text to reflect the actual company names they represent.

    3. Keep the existing grayscale/hover:grayscale-0 treatment and grid layout (3 cols mobile, 6 cols md+).

    4. Keep <Section id="sponsors"> wrapper.

    Exact framing copy to use: "Brands our founder has worked with"

    Build must pass: run `npm run build` in /Users/jon/bestcasestud.io to verify no errors.
  </action>
  <verify>
    <automated>cd /Users/jon/bestcasestud.io && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Logo strip renders with "Brands our founder has worked with" label above it. No broken image imports. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Update Services section with BCS value prop cards</name>
  <files>src/components/sections/Services.astro</files>
  <action>
    Rewrite the Services.astro cards array and description to reflect BCS's three value props. Do NOT change the ServiceCard component itself.

    New description (SectionTitle description prop):
    "Case studies are one of the highest-performing content types for B2B SaaS. Here's what they can do for you."

    New cards array (3 cards — reduce from 6 to 3, change grid to lg:grid-cols-3):
    ```
    cards = [
      {
        index: 1,
        titleTop: "Build",
        titleBottom: "Trust",
        img: c1,
        alt: "Build Trust",
        link: "#pricing",
      },
      {
        index: 2,
        titleTop: "Improve SEO",
        titleBottom: "& GEO",
        img: c2,
        alt: "Improve SEO and GEO",
        link: "#pricing",
      },
      {
        index: 3,
        titleTop: "Lower",
        titleBottom: "CAC",
        img: c3,
        alt: "Lower CAC",
        link: "#pricing",
      },
    ]
    ```

    Update the grid class from `lg:grid-cols-2 lg:grid-rows-3` to `lg:grid-cols-3` so three cards display in a single row on large screens. Remove the imports for c4, c5, c6 (unused).

    Update SectionTitle sectionTitle prop to: "Why Case Studies?"

    The link on each card should anchor to "#pricing" (the pricing section added in Plan 02).

    Note: The ServiceCard component only renders titleTop, titleBottom, img, and link — no body copy field exists. Do not attempt to add card description copy here.

    Build must pass: run `npm run build` in /Users/jon/bestcasestud.io.
  </action>
  <verify>
    <automated>cd /Users/jon/bestcasestud.io && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Services section shows "Why Case Studies?" heading with three cards: Build Trust (gray), Improve SEO & GEO (yellow), Lower CAC (dark). Grid is 3 columns on large screens. Build passes with no errors.</done>
</task>

</tasks>

<verification>
After all tasks complete:
- `npm run build` passes with zero errors
- `npm run dev` — visit http://localhost:4321, scroll below hero:
  - Sponsors strip shows "Brands our founder has worked with" above logos
  - Services section shows "Why Case Studies?" with 3 cards (not 6)
</verification>

<success_criteria>
- Logo strip has framing copy that attributes logos to Jon's background, not BCS client work (SPONS-01, SPONS-02)
- Three value prop cards visible: Build Trust, Improve SEO & GEO, Lower CAC (SERV-01, SERV-02, SERV-03)
- Build passes with no TypeScript or import errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-content/02-01-SUMMARY.md`
</output>
