---
phase: 03-conversion-close
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Contact.astro
  - src/components/ui/Footer.astro
autonomous: true
requirements: [CONT-01, CONT-02]

must_haves:
  truths:
    - "Visitors can book a 30-minute discovery call by clicking a calendar link or interacting with an embedded Calendly widget in the Contact section"
    - "The Contact section heading frames the call as no-pressure — not a sales pitch"
    - "The footer shows BCS social links (LinkedIn, Substack, Spotify, Apple Podcasts) with real hrefs"
    - "The footer copyright says Best Case Studio with the current year — no Positivus or template attribution"
    - "The footer does not contain a contact form, phone number, address, or newsletter signup"
  artifacts:
    - path: "src/components/sections/Contact.astro"
      provides: "Final CTA section with Calendly embed for 30-min discovery call booking"
      contains: "calendly"
    - path: "src/components/ui/Footer.astro"
      provides: "BCS footer with 4 social links and copyright"
      contains: "bestcasestud.io"
  key_links:
    - from: "src/components/sections/Contact.astro"
      to: "https://calendly.com"
      via: "Calendly inline embed script or link button"
      pattern: "calendly"
    - from: "src/components/ui/Footer.astro"
      to: "LinkedIn, Substack, Spotify, Apple Podcasts"
      via: "anchor href attributes"
      pattern: "linkedin\\.com|substack\\.com|spotify\\.com|podcasts\\.apple"
---

<objective>
Rewrite Contact.astro as a no-pressure discovery call CTA with an embedded Calendly calendar, and rewrite Footer.astro to reflect BCS brand identity with four social platform links.

Purpose: These are the final two conversion touch-points. The contact section must make booking feel easy and low-stakes. The footer must feel like a real agency, not a template.
Output: Contact.astro has Calendly embed. Footer.astro has BCS social links and copyright.
</objective>

<execution_context>
@/Users/jon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing component contracts the executor must respect -->

From src/components/sections/Section.astro:
```astro
const { id } = Astro.props;
// <section id={id} class="px-5 flex justify-center overflow-hidden lg:block">
//   <div class="w-full max-w-[1280px] mx-auto">...</div>
// </section>
```

From src/components/ui/SectionTitle.astro:
```astro
const { sectionTitle, description } = Astro.props;
```

From src/pages/index.astro — Contact.astro is already imported and rendered last before Footer:
```astro
// Contact.astro has id="contact" — the hero CTA "Book Discovery Call" anchors to #contact
// Do not change the section id.
```

Current Footer.astro structure to replace:
- Has Facebook, LinkedIn, Twitter social icons (wrong platforms — replace entirely)
- Has ShortForm component (remove — contact form not needed)
- Has placeholder email/phone/address (remove)
- Has Positivus copyright (replace)
- Keep the dark rounded-top container style: bg-dark text-gray py-[55px] px-[15px] lg:px-[60px] sm:rounded-t-[45px]

Brand colours: pink = var(--pink) = #ff7ae7
Calendly link for Jon: https://calendly.com/jon-bestcasestudio/30min
(If Jon has not set up Calendly yet, use this placeholder URL and note it in the summary)

Social platform URLs:
- LinkedIn: https://www.linkedin.com/in/jonmcgreevy/
- Substack: https://jonmcgreevy.substack.com
- Spotify: https://open.spotify.com/show/bestcasestudio (placeholder — update when live)
- Apple Podcasts: https://podcasts.apple.com/podcast/bestcasestudio (placeholder — update when live)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Contact.astro as discovery call CTA with Calendly embed</name>
  <files>src/components/sections/Contact.astro</files>
  <action>
    Replace the entire file. Remove the Form import. Keep Section and SectionTitle imports.

    Structure:
    - `<Section id="contact">` — keep id="contact" so hero anchor still works
    - `<div class="mb-[140px] mt-[140px]">`
    - SectionTitle:
      - sectionTitle="Book Your Discovery Call"
      - description="30 minutes. No hard sell. We'll find out if we're a good fit and answer any questions you have."
    - Below SectionTitle, add the Calendly inline embed:
      ```html
      <!-- Calendly inline widget -->
      <div
        class="calendly-inline-widget rounded-[20px] overflow-hidden"
        data-url="https://calendly.com/jon-bestcasestudio/30min"
        style="min-width:320px;height:700px;"
      ></div>
      <script
        type="text/javascript"
        src="https://assets.calendly.com/assets/external/widget.js"
        async
      ></script>
      ```

    Optionally add a fallback link below the embed in case JS is blocked:
    ```astro
    <p class="mt-6 text-sm text-zinc-500 text-center">
      Can't see the calendar?
      <a
        href="https://calendly.com/jon-bestcasestudio/30min"
        target="_blank"
        class="text-pink underline"
      >Book directly on Calendly</a>.
    </p>
    ```

    Do NOT include any form, phone number, or address.
  </action>
  <verify>
    <automated>cd /Users/jon/bestcasestud.io && grep -c "calendly" src/components/sections/Contact.astro && grep -c "Form" src/components/sections/Contact.astro || true</automated>
    First returns 2+ (calendly.com in data-url and script src). Second returns 0 (Form removed).
  </verify>
  <done>Contact.astro contains Calendly inline embed with data-url pointing to jon-bestcasestudio/30min, heading is "Book Your Discovery Call", no form or address present, id="contact" retained.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Footer.astro with BCS social links and copyright</name>
  <files>src/components/ui/Footer.astro</files>
  <action>
    Replace the entire file. Remove imports for Facebook, Twitter, ShortForm, and the unused Logo import. Keep Image import from astro:assets and Logo import if a BCS logo SVG exists at src/assets/logo-alt.svg (keep it for brand continuity).

    New structure — keep the outer dark rounded container, strip everything inside to just:
    1. Top row: Logo left, nav links centre, social icons right
    2. Divider line
    3. Copyright row

    **Nav links** (simplified — only valid in-page sections for v1):
    ```js
    const links = [
      { name: "Services", href: "#services" },
      { name: "Pricing", href: "#pricing" },
      { name: "Process", href: "#process" },
      { name: "FAQ", href: "#faq" },
    ];
    ```

    **Social icons** — use inline SVG paths (no imported SVG assets for new platforms):
    ```js
    const socials = [
      { name: "LinkedIn", href: "https://www.linkedin.com/in/jonmcgreevy/", label: "Jon McGreevy on LinkedIn" },
      { name: "Substack", href: "https://jonmcgreevy.substack.com", label: "Jon McGreevy on Substack" },
      { name: "Spotify", href: "https://open.spotify.com/show/bestcasestudio", label: "Best Case Studio on Spotify" },
      { name: "Apple Podcasts", href: "https://podcasts.apple.com/podcast/bestcasestudio", label: "Best Case Studio on Apple Podcasts" },
    ];
    ```

    For social icons, render platform name as text links styled as small uppercase labels (no SVG icons needed — cleaner and avoids missing asset errors):
    ```astro
    <ul class="flex flex-wrap gap-4">
      {socials.map((s) => (
        <li>
          <a
            href={s.href}
            target="_blank"
            rel="noopener noreferrer"
            aria-label={s.label}
            class="text-xs uppercase tracking-widest text-gray hover:text-pink transition-colors"
          >
            {s.name}
          </a>
        </li>
      ))}
    </ul>
    ```

    **Copyright row:**
    ```astro
    <span>© {new Date().getFullYear()} Best Case Studio. All rights reserved.</span>
    ```

    Remove: ShortForm, Form, contact info block (email/phone/address), Positivus credits, CC licence link, Facebook icon, Twitter icon.

    Keep: Dark background container, rounded-top styling, max-width wrapper, divider line.
  </action>
  <verify>
    <automated>cd /Users/jon/bestcasestud.io && grep -c "Best Case Studio" src/components/ui/Footer.astro && grep -c "Positivus\|ShortForm\|facebook" src/components/ui/Footer.astro || true</automated>
    First returns 1+ (copyright). Second returns 0 (all template content removed).
    Also: `npm run build` passes — no broken imports.
  </verify>
  <done>Footer.astro contains LinkedIn, Substack, Spotify, and Apple Podcasts links. Copyright reads "Best Case Studio". No ShortForm, no contact info block, no Positivus attribution. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with no errors
- `grep "calendly" src/components/sections/Contact.astro` returns matches
- `grep "Best Case Studio" src/components/ui/Footer.astro` returns at least 1 match
- `grep "Positivus\|ShortForm\|ShortForm\|facebook\\.com" src/components/ui/Footer.astro` returns no matches
- `grep "linkedin.com\|substack.com" src/components/ui/Footer.astro` returns 2 matches
</verification>

<success_criteria>
Contact.astro shows Calendly inline embed for a 30-min call with no-pressure heading. Footer.astro shows LinkedIn, Substack, Spotify, Apple Podcasts links and "Best Case Studio" copyright. All template placeholder content removed. Build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/03-conversion-close/03-03-SUMMARY.md` following the summary template.
</output>
